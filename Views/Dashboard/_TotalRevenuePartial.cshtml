@model Bus_ticket.ViewModels.DashboardRevenueViewModel

@{
    decimal kpiTotalRevenue = 0;
    int kpiTotalTickets = 0;
    decimal kpiAvgTicket = 0;
    
    int kpiCanceled = Model?.TotalCanceled ?? 0; 

    if (Model?.TableData?.Any() == true)
    {
        kpiTotalRevenue = Model.TableData.Sum(x => x.Amount);
        kpiTotalTickets = Model.TableData.Count; 
        kpiAvgTicket = kpiTotalTickets > 0 ? (kpiTotalRevenue / kpiTotalTickets) : 0;
    }

    var paymentSummaries = PaymentMethodDisplayHelper.BuildSummaries(Model?.TableData);
    var paymentChartData = PaymentMethodDisplayHelper.BuildChartData(paymentSummaries);

    var fromDate = DateTime.TryParse(ViewBag.FromDateValue as string, out var parsedFrom)
        ? parsedFrom.Date
        : DateTime.Today.AddDays(-30);
    var toDate = DateTime.TryParse(ViewBag.ToDateValue as string, out var parsedTo)
        ? parsedTo.Date
        : DateTime.Today;

    var periodGrouping = RevenuePeriodGroupingHelper.Build(Model?.TableData, fromDate, toDate);
    var lineLabels = periodGrouping.ChartGroups.Select(x => x.DisplayLabel).ToList();
    var lineValues = periodGrouping.ChartGroups.Select(x => x.Revenue).ToList();
    var lineQueryDates = periodGrouping.ChartGroups.Select(x => $"{x.QueryStartDate}|{x.QueryEndDate}").ToList();

    var busClassPalette = new[] { "#8b5cf6", "#10b981", "#0ea5e9", "#f59e0b", "#ef4444", "#6b7280" };
    var labelsSeat = Model?.ChartData.Select(c => c.Category).ToList() ?? new List<string>();
    var dataSeat = Model?.ChartData.Select(c => c.TotalRevenue).ToList() ?? new List<decimal>();
    var colorsSeat = labelsSeat.Select((_, index) => busClassPalette[index % busClassPalette.Length]).ToList();
}

<div class="mb-6 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
    <div>
        <h2 class="text-xl font-bold text-gray-900 dark:text-white">Báo cáo Tổng Doanh Thu</h2>
        <p class="text-sm text-gray-500 dark:text-gray-400">Phân bổ dòng tiền và hiệu suất bán vé toàn hệ thống</p>
    </div>
    <div class="flex items-center gap-3">
        <input type="text" id="date_range_TotalRevenue" readonly 
               class="bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-700 text-gray-900 dark:text-white text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-[260px] p-2.5 cursor-pointer shadow-sm" 
               placeholder="Chọn khoảng ngày...">
        
        <button onclick="exportTotalRevenueExcel()" class="bg-emerald-500 hover:bg-emerald-600 text-white font-semibold rounded-lg text-sm px-4 py-2.5 shadow-sm transition-colors flex items-center gap-2">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
            Xuất Excel
        </button>
    </div>
</div>

<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">
    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-5 shadow-sm flex items-center justify-between">
        <div>
            <p class="text-sm font-medium text-gray-500 dark:text-gray-400 mb-1">Tổng doanh thu</p>
            <h4 class="text-xl font-bold text-emerald-600 dark:text-emerald-400">@kpiTotalRevenue.ToString("N0") đ</h4>
        </div>
        <div class="p-3 bg-emerald-100 text-emerald-600 rounded-lg dark:bg-emerald-900/30 dark:text-emerald-400">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
        </div>
    </div>

    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-5 shadow-sm flex items-center justify-between">
        <div>
            <p class="text-sm font-medium text-gray-500 dark:text-gray-400 mb-1">Tổng vé bán</p>
            <h4 class="text-xl font-bold text-blue-600 dark:text-blue-400">@kpiTotalTickets.ToString("N0")</h4>
        </div>
        <div class="p-3 bg-blue-100 text-blue-600 rounded-lg dark:bg-blue-900/30 dark:text-blue-400">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 5v2m0 4v2m0 4v2M5 5a2 2 0 00-2 2v3a2 2 0 110 4v3a2 2 0 002 2h14a2 2 0 002-2v-3a2 2 0 110-4V7a2 2 0 00-2-2H5z"></path></svg>
        </div>
    </div>

    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-5 shadow-sm flex items-center justify-between">
        <div>
            <p class="text-sm font-medium text-gray-500 dark:text-gray-400 mb-1">Giá trị trung bình / Vé</p>
            <h4 class="text-xl font-bold text-amber-500 dark:text-amber-400">@kpiAvgTicket.ToString("N0") đ</h4>
        </div>
        <div class="p-3 bg-amber-100 text-amber-600 rounded-lg dark:bg-amber-900/30 dark:text-amber-400">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path></svg>
        </div>
    </div>

    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-5 shadow-sm flex items-center justify-between">
        <div>
            <p class="text-sm font-medium text-gray-500 dark:text-gray-400 mb-1">Vé hoàn / Hủy</p>
            <h4 class="text-xl font-bold text-red-500 dark:text-red-400">@kpiCanceled.ToString("N0")</h4>
        </div>
        <div class="p-3 bg-red-100 text-red-600 rounded-lg dark:bg-red-900/30 dark:text-red-400">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
        </div>
    </div>
</div>

<div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-5 shadow-sm mb-6 w-full">
    <div class="flex justify-between items-center mb-4">
        <div>
            <h3 class="text-md font-semibold text-gray-900 dark:text-white">@periodGrouping.ChartTitle</h3>
            <p class="text-xs text-gray-500 mt-1">@periodGrouping.ChartSubtitle</p>
        </div>
        <a asp-controller="Dashboard"
           asp-action="TotalRevenueDetail"
           asp-route-fromDate="@ViewBag.FromDateValue"
           asp-route-toDate="@ViewBag.ToDateValue"
           class="text-sm font-medium text-blue-600 hover:text-blue-700 hover:underline flex items-center gap-1">
            Mở trang chi tiết
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg>
        </a>
    </div>
    
    <div class="relative h-[320px] w-full flex justify-center">
        <canvas id="revenueLineChart"></canvas>
    </div>
</div>

<div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-5 shadow-sm">
        <h3 class="text-md font-semibold text-gray-900 dark:text-white mb-1">Tỷ trọng theo Hạng Xe</h3>
        <p class="text-xs text-gray-500 mb-4">Click vào phần biểu đồ để xem chi tiết theo hạng xe</p>
        <div class="relative h-[320px] w-full flex justify-center overflow-visible">
            <canvas id="totalRevenueChart"></canvas>
        </div>
    </div>

    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-5 shadow-sm">
        <h3 class="text-md font-semibold text-gray-900 dark:text-white mb-1">Theo phương thức thanh toán</h3>
        <p class="text-xs text-gray-500 mb-4">Click vào phần biểu đồ để xem chi tiết theo phương thức thanh toán</p>
        <div class="relative h-[320px] w-full flex justify-center overflow-visible">
            <canvas id="paymentMethodChart"></canvas>
        </div>
    </div>
</div>

<script>
    initializeDashboardDateRangePicker('TotalRevenue', '@ViewBag.FromDateValue', '@ViewBag.ToDateValue');

    var labelsSeat = @Html.Raw(Json.Serialize(labelsSeat));
    var dataSeat = @Html.Raw(Json.Serialize(dataSeat));
    var colorsSeat = @Html.Raw(Json.Serialize(colorsSeat));
    renderDashboardPieChart('totalRevenueChart', labelsSeat, dataSeat, colorsSeat, 'Tổng Doanh Thu', function (index, chartLabels) {
        const busClass = chartLabels[index];
        if (!busClass) return null;

        const params = new URLSearchParams();
        params.set('fromDate', '@ViewBag.FromDateValue');
        params.set('toDate', '@ViewBag.ToDateValue');
        params.set('busClass', busClass);
        return `/Dashboard/TotalRevenueDetail?${params.toString()}`;
    }, { valueSuffix: ' đ' });

    var labelsPayment = @Html.Raw(Json.Serialize(paymentChartData.Labels));
    var dataPayment = @Html.Raw(Json.Serialize(paymentChartData.Values));
    var colorsPayment = @Html.Raw(Json.Serialize(paymentChartData.Colors));
    renderDashboardPieChart('paymentMethodChart', labelsPayment, dataPayment, colorsPayment, 'Thanh toán', function (index, chartLabels) {
        const paymentMethod = chartLabels[index];
        if (!paymentMethod) return null;

        const params = new URLSearchParams();
        params.set('fromDate', '@ViewBag.FromDateValue');
        params.set('toDate', '@ViewBag.ToDateValue');
        params.set('paymentMethod', paymentMethod);
        return `/Dashboard/TotalRevenueDetail?${params.toString()}`;
    }, { valueSuffix: ' đ' });

    var lineLabels = @Html.Raw(Json.Serialize(lineLabels));
    var lineValues = @Html.Raw(Json.Serialize(lineValues));
    var lineQueryDates = @Html.Raw(Json.Serialize(lineQueryDates));

    var lineCanvas = document.getElementById('revenueLineChart');
    if (lineCanvas) {
        var lineCtx = lineCanvas.getContext('2d');
        
        if (window.dashboardCharts && window.dashboardCharts['revenueLineChart']) {
            window.dashboardCharts['revenueLineChart'].destroy();
        }

        window.dashboardCharts = window.dashboardCharts || {};
        window.dashboardCharts['revenueLineChart'] = new Chart(lineCtx, {
            type: 'line',
            data: {
                labels: lineLabels,
                datasets: [{
                    label: 'Doanh thu (VNĐ)',
                    data: lineValues,
                    borderColor: '#10b981', 
                    backgroundColor: 'rgba(16, 185, 129, 0.1)',
                    borderWidth: 2,
                    pointBackgroundColor: '#ffffff',
                    pointBorderColor: '#10b981',
                    pointBorderWidth: 2,
                    pointRadius: 4,
                    pointHoverRadius: 6,
                    fill: true,
                    tension: 0.3 
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: { display: false },
                    tooltip: {
                        callbacks: {
                            label: function(context) {
                                let value = context.raw || 0;
                                return 'Doanh thu: ' + value.toLocaleString('vi-VN') + ' đ';
                            }
                        }
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        ticks: {
                            callback: function(value) {
                                if (value >= 1000000) return (value / 1000000) + ' Tr';
                                if (value >= 1000) return (value / 1000) + ' K';
                                return value;
                            }
                        }
                    }
                },
                onClick: function(event, elements) {
                    if (elements.length > 0) {
                        var index = elements[0].index;
                        var dates = lineQueryDates[index].split('|');
                        var url = `/Dashboard/TotalRevenueDetail?fromDate=${dates[0]}&toDate=${dates[1]}`;
                        window.location.href = url;
                    }
                },
                onHover: function(event, elements) {
                    event.native.target.style.cursor = elements.length ? 'pointer' : 'default';
                }
            }
        });
    }

    function exportTotalRevenueExcel() {
        var start = currentFromDate || '@ViewBag.FromDateValue';
        var end = currentToDate || '@ViewBag.ToDateValue';
        window.location.href = `/Dashboard/ExportTotalRevenue?startDate=${start}&endDate=${end}`;
    }
</script>